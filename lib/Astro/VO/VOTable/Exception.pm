package Astro::VO::VOTable::Exception;

use strict;
use warnings;

use custom::failures::x::alias qw(
  parameter::unknown
  parameter::missing
  parameter::value::illegal
);

our $VERSION = '1.1';


1;
